<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
            display: grid;
            place-items: center;
            background: linear-gradient(135deg, #0f0f1a 0%, #1a1a2e 50%, #16213e 100%);
        }

        .card {
            width: 380px;
            padding: 40px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            backdrop-filter: blur(20px);
        }

        h1 {
            color: #fff;
            font-size: 28px;
            text-align: center;
            margin-bottom: 8px;
        }

        .sub {
            color: #666;
            font-size: 14px;
            text-align: center;
            margin-bottom: 32px;
        }

        .creds {
            background: rgba(99, 102, 241, 0.1);
            border: 1px solid rgba(99, 102, 241, 0.3);
            border-radius: 12px;
            padding: 16px;
            margin-bottom: 28px;
            font-size: 12px;
        }

        .creds-title {
            color: #818cf8;
            font-weight: 600;
            margin-bottom: 12px;
        }

        .cred-row {
            display: flex;
            justify-content: space-between;
            color: #a5b4fc;
            padding: 6px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .cred-row:last-child {
            border: none;
        }

        .cred-row span:first-child {
            color: #666;
        }

        .cred-row code {
            background: rgba(0, 0, 0, 0.3);
            padding: 2px 8px;
            border-radius: 4px;
            cursor: pointer;
        }

        input {
            width: 100%;
            padding: 16px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            color: #fff;
            font-size: 14px;
            margin-bottom: 16px;
            outline: none;
            transition: 0.3s;
        }

        input:focus {
            border-color: #6366f1;
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
        }

        input::placeholder {
            color: #555;
        }

        .pass-wrap {
            position: relative;
        }

        .pass-wrap input {
            padding-right: 50px;
        }

        .toggle {
            position: absolute;
            right: 16px;
            top: 16px;
            background: none;
            border: none;
            color: #555;
            cursor: pointer;
            font-size: 16px;
        }

        .toggle:hover {
            color: #fff;
        }

        button[type="submit"] {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #6366f1, #8b5cf6);
            border: none;
            border-radius: 12px;
            color: #fff;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 8px;
        }

        button[type="submit"]:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(99, 102, 241, 0.4);
        }

        .divider {
            text-align: center;
            color: #444;
            font-size: 12px;
            margin: 24px 0;
        }

        .social {
            display: flex;
            gap: 12px;
            justify-content: center;
        }

        .social button {
            width: 60px;
            height: 44px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            color: #888;
            font-size: 18px;
            cursor: pointer;
            transition: 0.2s;
        }

        .social button:hover {
            background: #fff;
            color: #000;
        }

        .msg {
            padding: 12px;
            border-radius: 8px;
            text-align: center;
            margin-bottom: 16px;
            font-size: 13px;
            display: none;
        }

        .msg.error {
            display: block;
            background: rgba(239, 68, 68, 0.1);
            color: #f87171;
            border: 1px solid rgba(239, 68, 68, 0.3);
        }

        .msg.success {
            display: block;
            background: rgba(34, 197, 94, 0.1);
            color: #4ade80;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }
    </style>
</head>

<body>
    <div class="card">
        <h1>Welcome</h1>
        <p class="sub">Sign in to continue</p>

        <div class="creds">
            <div class="creds-title">🔑 Demo Credentials</div>
            <div class="cred-row"><span>Admin:</span>
                <code>admin1 / adminpassword</code>
            </div>
            <div class="cred-row"><span>User:</span>
                <code>user1 / userpassword</code>
            </div>
        </div>

        <div class="msg" id="msg"></div>
        <form id="form" action="/authenticate/visitor" method="POST">
            <input type="text" id="uid" placeholder="User ID" name="username" required>
            <div class="pass-wrap">
                <input type="password" id="pwd" placeholder="Password" name="password" required>
                <button type="button" class="toggle">👁</button>
            </div>
            <button type="submit">Sign In</button>
        </form>

        <div class="divider">or</div>
        <div class="social">
            <button>G</button>
            <button></button>
            <button>⌘</button>
        </div>
    </div>
</body>

</html>